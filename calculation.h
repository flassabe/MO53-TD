#ifndef CALCULATION_H
#define CALCULATION_H
#include <iostream>
template <typename T>
class calculation{
/*
protected:
    virtual T do_average(T*) = 0;
    virtual T do_sort_table(bool, T) = 0;
    virtual T do_table_count(T*) = 0;
    virtual T table_sum(T*) = 0;
    virtual T do_moving_average(size_t) = 0;
*/
};

#endif // CALCULATION_H
